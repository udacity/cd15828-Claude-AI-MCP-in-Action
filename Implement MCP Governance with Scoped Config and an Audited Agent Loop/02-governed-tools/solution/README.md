# Solution — Exercise 2: Custom Servers, Tool Descriptions & a Schema Resource

The completed `decision.py`, `contracts.py`, `servers.py`, and `schema.py`. The audited-loop
modules (`audit.py`, `bridge.py`, `loop.py`) are still stubbed, so verify with the scoped
command.

```bash
.venv/bin/pytest tests/test_servers.py tests/test_resource.py tests/test_decision.py   # 54 passed cumulatively
.venv/bin/mcp-governance decide
```
